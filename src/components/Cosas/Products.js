/*import React from "react";
import Product from "./Product";
import './Card.css'

const Products = ({ properties}) => {
  return (
    <div className="Products">
      {properties.map((properties) => (
        <Product {...properties} key={properties.id} />
      ))}
    </div>
  );
};

export default Products;*/